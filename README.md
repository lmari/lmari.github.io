# This is my personal homepage...

... presenting a summary of my activities, interests, outcomes.

(These pages should be browsed from https://lmari.github.io)

Luca Mari
<!--
In this directory, to update the site:
    git add --all
    git commit -m "a commit message"
    git push -u origin main
-->

<!--
In this directory, to serve locally the site (Fedora):
    sudo dnf install gcc-c++

    sudo dnf install rbenv ruby-build-rbenv gcc make \
    openssl-devel readline-devel zlib-devel libyaml-devel libffi-devel

    sudo dnf install ruby-devel
    
    rbenv install 3.4.10
    rbenv local 3.4.10
    eval "$(rbenv init -)"
(per Fedora)

    BUNDLE_PATH=vendor/bundle bundle install
if required, and then:
    bash preview.sh

In this directory, to generate a static version of the site (in _site):
    BUNDLE_PATH=vendor/bundle bundle exec jekyll build
-->
